import styled from 'styled-components'

export const Form = styled.form`
  max-width: 547px;
  width: 100%;
  font-weight: bold;
  font-size: 14px;

  input {
    margin-bottom: 16px;
  }
`

export const Opcoes = styled.div`
  margin-bottom: 8px;

  p {
    margin-bottom: 8px;
  }

  label {
    margin-right: 8px;
  }
`

export const Opcao = styled.div`
  display: inline;
  /* Capitalize deixa a primeira letra das palavras em Upper  */
  text-transform: capitalize;
`
